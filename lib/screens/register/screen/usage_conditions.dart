import 'package:flutter/material.dart';

class UsageConditions extends StatelessWidget {
  const UsageConditions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text(''),
      ),
    );
  }
}
